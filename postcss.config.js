/**
 * PostCSS configuration file
 * Used by Vite/Tailwind to process CSS
 */

module.exports = {
  plugins: {
    // Tailwind CSS plugin that enables utility classes processing
    tailwindcss: {},

    // Autoprefixer automatically adds vendor prefixes for browser compatibility
    autoprefixer: {},
  },
};