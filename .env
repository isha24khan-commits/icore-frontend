# Base URL for the backend API
# This environment variable is used by the React frontend to know
# where to send API requests (Axios / fetch calls).

# In this case, the backend is deployed on Render

REACT_APP_BACKEND_URL=https://icore-backend.onrender.com
